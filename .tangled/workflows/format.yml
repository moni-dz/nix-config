when:
  - event: ["push", "manual", "pull_request"]
    branch: ["master"]

engine: nixery

steps:
  - name: "format check"
    command: "nix --extra-experimental-features 'ca-derivations nix-command flakes' fmt -- --ci"